name = "busexplorer"
version = "1snapshot"
url = SVNREPURL .. "/openbus/utils/busexplorer/trunk"
dependencies = { 
  "openbus-java == 2.0.0.2",
}

local install_path = name .. "-" .. version

build = {
  type = "maven",
  javadoc = true,
  variables = { 
  },
}

install_files = {
  ["BusExplorer/doc/README"] = install_path,
  ["BusExplorer/etc/busexplorer.conf"] = install_path,
  ["BusExplorer/target/busexplorer-1-SNAPSHOT-jar-with-dependencies.jar"] = install_path,
}
